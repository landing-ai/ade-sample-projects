Feedback on the application:


1. For the 3 color options offered for the highlighting chnage the white option to red.
2. Do not show the warning message in the upper right if extract returns a warning
3. Make the pre-filled values in the write in fields a lighter grey. If the users actually types something different, make it blue and bold.
4. Can you make the left pane slightly narrower and the right pane slightly wider (or make this adjsutable by the user).
5. Increase the font size on the right pane (or give the user control to increase the font.)
6. Since this is a demo app, provide a button in the lower right to clear all work and reset. This should clear the outputs fo rthe selected folder only.

7. The UI doesn't have a goo way to handle array of object results. It shows only the [0] element. Give some thought to how this can be improved, but it is not critical for a demo app.