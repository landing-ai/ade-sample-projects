landingai-ade>=1.16.0
# [secure-local-storage] adds keyring so SSO (externalbrowser) tokens cache
# and you're prompted once, not on every connection.
snowflake-connector-python[secure-local-storage]
pydantic
pydantic-settings
python-dotenv
notebook
ipykernel
tqdm          # progress bars during the streaming run
ipywidgets    # nice in-notebook progress bars for tqdm.auto
